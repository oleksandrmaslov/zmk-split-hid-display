# Example GitHub Actions build matrix entries for a split keyboard using this module.
# Adjust the board and base shield names to match your keyboard.
---
include:
  - board: nice_nano//zmk
    shield: corne_left nice_view_hid raw_hid

  - board: nice_nano//zmk
    shield: corne_right nice_view_hid raw_hid
